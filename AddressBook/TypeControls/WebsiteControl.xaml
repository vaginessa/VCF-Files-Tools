<UserControl x:Class="Microsoft.Communications.Contacts.Widgets.TypeControls.WebsiteControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid HorizontalAlignment="Stretch" VerticalAlignment="Top">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="1*"/>
      <ColumnDefinition Width="2*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Label Content="Personal:" Grid.Row="0" HorizontalAlignment="Right"/>
    <TextBox Grid.Column="1" Text="{Binding Path=PersonalWebsite}"/>

    <Label Content="Work:" Grid.Row="1" HorizontalAlignment="Right"/>
    <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Path=WorkWebsite}"/>

    <Label Content="Web Feed (RSS):" Grid.Row="2" HorizontalAlignment="Right"/>
    <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding Path=WebFeed}"/>
  </Grid>
</UserControl>
